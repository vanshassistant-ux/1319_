{
  "id": "python-programming:foundations:return-values",
  "subjectSlug": "python-programming",
  "unitSlug": "foundations",
  "slug": "return-values",
  "title": "Return Values",
  "editorialStatus": "READY_TO_PUBLISH",
  "estimatedMinutes": 10,
  "tags": [
    "python",
    "functions",
    "return",
    "none",
    "control-flow"
  ],
  "in30Seconds": "A return value is the result a function sends back to the code that called it. In Python, `return expression` evaluates the expression, immediately ends that function call, and makes the result available to the caller. A bare `return`, or reaching the end of a function without `return`, produces `None`. Returning a value differs from printing it: returned data can be stored, combined, or tested by other code.",
  "whyItMatters": "Return values give functions useful, composable outputs. A caller can assign a returned result, pass it into another function, or make a decision from it without tying the function to a screen or file. This separation makes small programs easier to test: call a function with known data and compare its returned result with the expected one. It also makes a function's contract clearer, because readers can identify the result it promises and every path that produces it.",
  "learningObjectives": [
    "Define a return value and explain how a caller can use it.",
    "Trace how `return expression` evaluates an expression and ends a function call.",
    "Distinguish returning a value from printing a value.",
    "Explain why a function with no value expression returns `None` in Python.",
    "Analyze a conditional function to find a path that accidentally returns `None`."
  ],
  "collegeVersion": [
    {
      "heading": "A return statement completes a call",
      "body": "A function call is an expression: it runs the function body and then produces a result at the point where the call appeared. Python's `return` statement supplies that result. In `return total * rate`, Python evaluates `total * rate`, passes that value to the caller, and leaves the current function call immediately. Statements later in the same function body are not executed on that path. The caller can place the result in a name, use it in an arithmetic expression, pass it to another call, or simply ignore it.\n\nThe word immediately matters when reading code. A function can contain several return statements, often in different branches, but a particular call follows only one path. Once that path executes return, the call is over. This is useful for guard clauses: after detecting invalid input, a function may return an appropriate result rather than nesting the remaining work inside another `else`. It also means that a statement written after an unconditional `return` is unreachable during that call. An editor may flag such code, but Python's language rule is simpler: return transfers control back to the caller."
    },
    {
      "heading": "An expression, a bare return, and the end of a function",
      "body": "The language reference specifies two related cases. If a return statement has an expression list, Python evaluates it; if it has no expression list, Python substitutes `None`. Falling off the end of a function body likewise gives the caller `None`. `None` is a real singleton object used here to signal that the function did not provide a meaningful computed result. It is not the number zero, the empty string, or an empty list, even though all of those can be false in a Boolean context. Test for it by identity when needed: `result is None`.\n\nThis default helps functions whose purpose is an action, such as a small function that prints a label or changes an object. It is also a frequent bug signal. Suppose `discount(price)` returns a reduced price only when `price > 100` and then reaches the end otherwise. The high-price path returns a number; the other path returns `None`. If a caller adds that result to a number, Python raises a TypeError rather than silently inventing a price. A function that promises a numeric result should normally return a numeric result on every intended path, including an explicit unchanged value when that expresses the policy."
    },
    {
      "heading": "Return is not print",
      "body": "`print` sends a text representation to standard output and itself returns `None`. `return` transfers a value to the caller. Those effects can look similar in an interactive session because both may cause a number to appear nearby, but they support different designs. Consider `def show_total(x, y): print(x + y)`. Calling it displays a total, but `saved = show_total(2, 3)` binds `saved` to `None`, not 5. In contrast, `def total(x, y): return x + y` lets the caller decide whether to print, format, compare, or reuse 5.\n\nSeparating computation from presentation is practical. A `total` function can be tested with an equality assertion without capturing screen output. A user-interface layer can choose a message and formatting suitable for its setting. This lesson does not prescribe that functions never print; programs need output. It identifies the interface question: is the function chiefly reporting data to its caller, producing an external effect, or intentionally doing both? A clear name and an explicit return policy reduce surprises for callers."
    },
    {
      "heading": "One result can hold structured data",
      "body": "A return statement is not restricted to a single numeric value. It can return any Python object, including a string, list, dictionary, or tuple. Comma-separated expressions in a return statement form a tuple, so `return low, high` gives the caller one tuple object that can be unpacked with `low, high = bounds(values)`. The important design question is not a mythic limit on the number of things a function can return; it is whether the returned object clearly communicates the function's result.\n\nFor an introductory function, a direct value often makes the contract obvious. For related values, a tuple or a named structure can make the relationship explicit. Avoid using an accidental mixture of shapes, such as returning an integer on one branch and a two-item tuple on another unless callers are deliberately designed to handle both. Likewise, avoid making callers infer success from a printed message. Choose a return type and ensure every normal path satisfies that contract. Parameters, argument matching, and local-name visibility are separate topics; here the concern is the value that travels outward after a call."
    }
  ],
  "vocabulary": [
    {
      "term": "return value",
      "definition": "The value a function call provides to the code that invoked it after the function completes."
    },
    {
      "term": "return statement",
      "definition": "A statement that optionally evaluates an expression, ends the current function call, and supplies its result to the caller."
    },
    {
      "term": "caller",
      "definition": "The code that invokes a function and receives control again when that function call completes."
    },
    {
      "term": "None",
      "definition": "Python's singleton object used to represent the absence of a useful value; it is returned by a function that has no value expression to return."
    },
    {
      "term": "bare return",
      "definition": "A `return` statement with no expression, which ends the function call and returns `None`."
    },
    {
      "term": "unreachable code",
      "definition": "Statements that cannot run on a particular control-flow path, such as statements after an unconditional return in the same block."
    },
    {
      "term": "function contract",
      "definition": "The documented or implied agreement about a function's inputs, outputs, and observable behavior."
    },
    {
      "term": "tuple",
      "definition": "An ordered, immutable Python collection; comma-separated expressions returned together form a tuple."
    }
  ],
  "eli10": {
    "simplifiedExplanation": "Imagine asking a library clerk to find a book. You give the clerk a request, and the clerk hands a book back across the desk. That book is like a return value: the person who asked can now read it, put it on a shelf, or give it to someone else. A function can do work and hand its answer back in the same way. `return 5` means “this call is finished; here is 5.” If the clerk finishes without handing you anything, you have no book. Python represents that no-result outcome with `None`.",
    "analogy": "A return value is an item a clerk hands back to the person who made a request; the caller decides what to do with the item.",
    "analogyLimits": "A function does not literally pass an object through space, and `None` is still a valid Python object rather than an empty hand. Printing is also unlike returning: it is more like the clerk announcing a title aloud, which does not give the requester a value to reuse in another calculation."
  },
  "workedExample": "This function applies a simple membership discount and returns a price on both possible paths:\n\n```python\ndef final_price(price, member):\n    if member:\n        return price * 0.9\n    return price\n\nmember_price = final_price(80, True)\nguest_price = final_price(80, False)\nprint(member_price, guest_price)\n```\n\nThe first call takes the `if` path: `80 * 0.9` evaluates to `72.0`, and return ends that call. The second call skips the `if` body and reaches `return price`, so it returns `80`. Running this exact code with python3 prints `72.0 80`. If the final `return price` were removed, the non-member call would reach the end of the function and return `None`, violating the function's intended all-cases price contract.",
  "commonMistakes": [
    {
      "mistake": "Using `print(value)` when later code needs the value.",
      "correction": "Return the value from the computation and let the caller decide whether to print it."
    },
    {
      "mistake": "Assuming a function without `return` returns the last expression it calculated.",
      "correction": "Python returns `None` when execution reaches the end without a value expression."
    },
    {
      "mistake": "Writing statements after an unconditional return and expecting them to execute.",
      "correction": "Move needed work before return or place it in a different reachable branch."
    },
    {
      "mistake": "Returning a value in only one conditional branch when every call needs a result.",
      "correction": "Trace every intended path and return an appropriate value on each one."
    }
  ],
  "compareContrast": [
    {
      "a": "return",
      "b": "print",
      "distinction": "Return gives a value to the caller for later use; print writes a representation to standard output and returns None."
    },
    {
      "a": "`return value`",
      "b": "bare `return`",
      "distinction": "The first evaluates and supplies a value; the second ends the call and supplies None."
    },
    {
      "a": "`None`",
      "b": "an empty collection",
      "distinction": "None signals no useful result, while an empty list or string is an existing value with no elements or characters."
    }
  ],
  "keyTakeaway": "A return statement is a function's outward handoff: it ends the current call and gives a value to the caller. Design functions so their intended paths return clear, consistent results, and do not confuse returning data with printing it.",
  "questionBank": [
    {
      "id": "python-programming:foundations:return-values:1",
      "prompt": "What does a Python function return when it reaches the end of its body without executing a `return` statement?",
      "options": [
        "The last expression it evaluated",
        "An empty string",
        "None",
        "A syntax error"
      ],
      "correctIndex": 2,
      "explanation": "Reaching the end of a function without a return expression gives the caller None. Python does not automatically return the last calculated expression, and this situation is not a syntax error.",
      "difficulty": "recall",
      "skill": "Identify Python's default function result",
      "sourceIds": [
        "python-reference-assignment-statements"
      ]
    },
    {
      "id": "python-programming:foundations:return-values:2",
      "prompt": "What happens after this path executes `return total * rate` inside a function?",
      "options": [
        "The expression is evaluated, its value is sent to the caller, and the current call ends",
        "Python prints the value and continues with the next statement in the function",
        "The value is assigned automatically to a global name named `return`",
        "The function restarts from its first line with the new value"
      ],
      "correctIndex": 0,
      "explanation": "A return statement evaluates its expression, passes the result to the caller, and exits the current function call. It neither prints automatically nor assigns a global name or restarts the function.",
      "difficulty": "understanding",
      "skill": "Explain return control flow",
      "sourceIds": [
        "python-reference-assignment-statements"
      ]
    },
    {
      "id": "python-programming:foundations:return-values:3",
      "prompt": "What value is assigned to `saved` by this code? `def show(x): print(x + 1)` followed by `saved = show(4)`",
      "options": [
        "4",
        "5",
        "The text `5`",
        "None"
      ],
      "correctIndex": 3,
      "explanation": "show prints 5 as a side effect, but it has no return statement, so its call evaluates to None and saved receives None. Printing output is not the same as returning a value.",
      "difficulty": "application",
      "skill": "Distinguish print from return",
      "sourceIds": [
        "python-reference-assignment-statements",
        "python-tutorial-controlflow"
      ]
    },
    {
      "id": "python-programming:foundations:return-values:4",
      "prompt": "A function is meant to return a number for every nonnegative input. Which version meets that contract?",
      "options": [
        "`if n > 0: return n * 2`",
        "`if n > 0:\n    return n * 2\nreturn 0`",
        "`print(n * 2)`",
        "`return; return n * 2`"
      ],
      "correctIndex": 1,
      "explanation": "The second version returns n * 2 for positive n and 0 for n equal to 0, so every nonnegative input gets a number. The first falls through to None for 0; print returns None; the final return in option D is unreachable.",
      "difficulty": "analysis",
      "skill": "Check return paths against a contract",
      "sourceIds": [
        "python-reference-assignment-statements",
        "python-tutorial-controlflow"
      ]
    },
    {
      "id": "python-programming:foundations:return-values:5",
      "prompt": "Why is code directly after an unconditional `return` in the same function block not run on that path?",
      "options": [
        "Python deletes the later code when it loads the file",
        "The return statement ends the current function call and transfers control back to its caller",
        "The later code is converted into a comment",
        "Only `print` can run after return"
      ],
      "correctIndex": 1,
      "explanation": "Once return executes, the active function call has ended, so later statements in that same path are unreachable. Python does not delete or comment out the source, and print has no exception to the control-flow rule.",
      "difficulty": "understanding",
      "skill": "Identify unreachable control flow",
      "sourceIds": [
        "python-reference-assignment-statements"
      ]
    }
  ],
  "quickCheck": [
    "What does a Python function return when it reaches the end of its body without executing a `return` statement?",
    "What happens after this path executes `return total * rate` inside a function?",
    "What value is assigned to `saved` by this code? `def show(x): print(x + 1)` followed by `saved = show(4)`"
  ],
  "relatedTopics": [
    "python-programming:foundations:functions",
    "python-programming:foundations:function-parameters",
    "python-programming:foundations:scope",
    "python-programming:foundations:conditionals",
    "python-programming:foundations:basic-testing"
  ],
  "sources": [
    "python-reference-assignment-statements",
    "python-tutorial-controlflow"
  ],
  "provenance": {
    "sourceProject": "EliExplains original synthesis",
    "researchStatus": "source-verified",
    "rightsStatus": "reference-only sources; no source prose adapted",
    "researchedAt": "2026-08-19",
    "transformation": "Official Python language-reference and tutorial material was independently read; examples and explanations were written from scratch and executed in python3."
  },
  "contentHash": "dc684ad4db4c8d83",
  "status": "source-verified",
  "public": false
}
