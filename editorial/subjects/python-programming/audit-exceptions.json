{
  "schemaVersion": 1,
  "subjectSlug": "python-programming",
  "accepted": [
    { "match": "\"rebinding\" defined differently in variables and tuples", "reason": "Compatible depth difference: Variables defines rebinding as a name-binding operation; Tuples uses it to contrast assigning a new tuple with mutating an existing immutable tuple." },
    { "match": "\"object\" defined differently in variables and object-oriented-programming", "reason": "Compatible uses: Variables explains Python runtime objects and name references; OOP uses object for an instance in a design discussion." },
    { "match": "\"immutable\" defined differently in strings and tuples", "reason": "Both definitions correctly describe immutable sequence values in their own type-specific lesson." },
    { "match": "\"immutable\" defined differently in tuples and string-methods", "reason": "Both definitions correctly describe an immutable receiver and the resulting new value in their own type-specific lesson." },
    { "match": "\"condition\" defined differently in conditionals and while-loops", "reason": "Compatible context-specific explanations: branch selection versus repetition control." },
    { "match": "\"iteration\" defined differently in for-loops and while-loops", "reason": "Both are correct context-specific definitions of one pass through a loop." },
    { "match": "\"iteration\" defined differently in while-loops and reading-files", "reason": "The loop lesson owns control-flow iteration; Reading Files correctly uses iterable file lines." },
    { "match": "\"method\" defined differently in string-methods and classes-and-objects", "reason": "Genuine compatible terminology: a type-provided dot-call operation versus a function defined in a class." },
    { "match": "python-syntax.md:36 unbalanced parentheses", "reason": "Literal multiline Python source deliberately opens a parenthesized expression on this line and closes it later; the line-based counter is not a syntax defect." },
    { "match": "python-syntax.md:39 unbalanced parentheses", "reason": "Literal multiline Python source deliberately closes a parenthesized expression opened earlier; the line-based counter is not a syntax defect." },
    { "match": "python-syntax.md:91 unbalanced parentheses", "reason": "Literal multiline Python source deliberately opens a parenthesized expression on this line and closes it later; the line-based counter is not a syntax defect." },
    { "match": "python-syntax.md:94 unbalanced parentheses", "reason": "Literal multiline Python source deliberately closes a parenthesized expression opened earlier; the line-based counter is not a syntax defect." }
    ,{ "match": "installing-python.md:5 space before punctuation", "reason": "False positive: the valid command `python3 -m venv .venv` must contain a space before the `.venv` directory argument." }
    ,{ "match": "installing-python.md:35 space before punctuation", "reason": "False positive: the valid command `python3 -m venv .venv` must contain a space before the `.venv` directory argument." }
    ,{ "match": "installing-python.md:68 space before punctuation", "reason": "False positive: the valid command `python3 -m venv .venv` must contain a space before the `.venv` directory argument." }
    ,{ "match": "string-methods.md:41 missing space after comma", "reason": "False positive: `red,green,blue` is a deliberate literal comma-separated input for demonstrating `split(',')`." }
  ]
}
